node_modules
dist
*.local
.DS_Store

# Base de datos SQLite (datos locales, no se versionan)
*.db
*.db-shm
*.db-wal

# Artefactos de build de TypeScript
*.tsbuildinfo

# Estado local de Wrangler/Miniflare (D1 local, caché)
.wrangler

# Secretos de desarrollo local (no se versionan)
.dev.vars
.dev.vars.*
